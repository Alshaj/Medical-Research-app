/**
 * Seed initial data - Empty implementation to ensure clean production database state without mockup data.
 */
export async function seedInitialDataIfEmpty(): Promise<void> {
  // No mockup data automatically seeded
}
